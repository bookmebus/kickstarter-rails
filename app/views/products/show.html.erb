<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Product Details</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.16/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
  <header class="bg-white p-4 shadow-md">
    <!-- Header content goes here -->
  </header>
  
  <main class="max-w-lg mx-auto">
    <div class="py-5 px-7 space-y-5 bg-gray-200 border-md rounded-md">
      <!-- Product Name -->
      <div class="product-details">
        <strong class="text-lg font-semibold">Product Name:</strong>
        <p><%= @product.name %></p>
      </div>
      <!-- Product Description -->
      <div class="product-details">
        <strong class="text-lg font-semibold">Description:</strong>
        <p><%= @product.description %></p>
      </div>
      <!-- Loop through product variants -->
      <% @product.variants.each do |variant| %>
        <!-- ... (variant details) ... -->
      <% end %>
      <!-- Common Product Details Block -->
      <div class="product-details">
        <strong class="text-lg font-semibold">Name:</strong>
        <p><%= @product.name %></p>
        <strong class="text-lg font-semibold">Price:</strong>
        <p class="text-gray-600">$<%= @product.variants.first.price %></p>
        <form action="<%= cart_add_path %>" method="post" class="mt-4">
          <%= hidden_field_tag :id, @product.id %>
          <label for="quantity" class="block font-semibold mb-1">Quantity:</label>
          <input type="number" name="quantity" value="1" min="1" class="w-16 py-1 px-2 border rounded text-gray-700">
          <button type="submit" class="mt-2 btn-primary px-4 py-2 rounded-md bg-blue-500 text-white hover:bg-blue-600">Add To Cart</button>
        </form>
      </div>
      
      <!-- Shopping Cart Icon -->
      <div class="flex justify-end mt-4">
        <a href="<%= cart_path %>" class="flex items-center text-blue-500 hover:text-blue-700">
          View Cart
        </a>
      </div>
    </div>
  </main>
  
  <footer class="bg-white p-4 mt-6 shadow-md">
    <!-- Footer content goes here -->
  </footer>
</body>
</html>
